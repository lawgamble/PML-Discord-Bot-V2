import { Timer, Time, TimerOptions } from './index';

const fn = (a) => {
    let sum = 0;
    for (let i = 0; i < 10000000; i += 1) {
      sum += a * i;
    }
    return sum;
  }

  const x = new Timer({ label: 'xx' });
  x.start();
  for (let i = 0; i < 10000000; i ++){}
  console.log(x.time());
  x.pause();
  for (let i = 0; i < 10000000; i ++){}
  console.log(x.pauseTime());
  const t: Time = Timer.benchmark(fn.bind(fn, 5)).time();
  console.log(x);
